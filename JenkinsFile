pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
        stage('Build') {
            steps {
                sh 'python -m py_compile fact.py'
            }
        }
        stage('Test') {
            steps {
                sh 'python fact.py'
            }
        }
    }
}

